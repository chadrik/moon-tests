tasks:
    editme:
        command: 'echo "edit this to test if projectB:generate is triggered: x"'
    lint:
        command: 'printf "\ndef foo():\n    pass\n" > foo.py'
        inputs:
            - foo.py

